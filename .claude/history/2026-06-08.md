# 2026-06-08 — Daily activity log

- kyc-onboarding | DEEP-PLAN | complexity:7/10 | rounds:2 | drawer:project_kyc_onboarding_planning_round_2026_06_08 | Deepened T2–T9 design; 6 red-team holes resolved (archive-not-delete, more_info per-field merge, earliest-step routing, HYDRATE exempt from transition guard, tie→local, bounded poll + cancelledRef); user decisions: rejected=terminal, DOB 18+.

## Session 2026-06-08 — /ticket-flow build T2–T9

Built the full KYC state machine T2→T9 in dependency order on branch `feat/kyc-state-machine-t2-t9` (greenfield; no Jira; copilot off; frontend-only). Treated the approved `.claude/findings/kyc-onboarding/PLAN.md` as the deep-plan output (no re-plan). 12 commits, one per unit + fixes.

### ✅ KYC-T2…T9 — KYC onboarding state machine — Shipped (local branch)

| Field | Value |
| ----- | ----- |
| Branch | `feat/kyc-state-machine-t2-t9` → `main` (not pushed — awaiting user) |
| Files | 32 changed, +3421 / −29 |
| Tests | 102 passed (10 suites): E1 validation, E2 transitions, E3 storage, E4 conflict, E5 routing/prefill, E6 reducer↔service, E7 service, E8 poll hook, redaction, App integration |
| Typecheck | clean (`tsc --noEmit`) |
| Coverage | 96% stmts / 88% branches on pure-logic modules; gate ≥80% enforced |
| Security review | CLEAN — no PII in logs (only `console.*` routes through `redact()`), no secrets, no network, plaintext storage documented |

**Units:** T2 types+maps+`earliestStepFor`; T3 deterministic fake service (sentinels, idempotency, NETFAIL-not-cached); T4 transitions+conflictResolution+reducer carrying all 6 red-team fixes (each with a discriminating test); T5 versioned draftStorage (archive-not-delete) + redaction; T6 UI projection (5 screens + App orchestration) + coverage gate; T7 bounded poll hook (errors count toward bound, cancelledRef); T9 per-step validation + 18+ DOB.

**QA (code-review + advisor) caught 3 real integration-layer bugs that unit tests missed** — all in App/screens (excluded from coverage):
1. Cold-start resume lost the draft (in-memory service's fresh `not_started` default beat the persisted draft on timestamp). Fixed in `reconcile`: `not_started` server ⇒ resume local. Regression test.
2. `requires_more_info` correction loop lost edits + infinite-bounced (stale idempotency cache; corrections never persisted). Fixed: service accepts more_info edits + invalidates cache; App persists before resubmit; `seedKyc` gives the service session memory. Regression test.
3. Transient poll error tore down the loop in-app (Hole 6a dead despite E8 green) — `active` gated on `machineStatus !== 'error'` + `POLL_ERROR` set `'error'`. Fixed; added App integration test.

**Deliberate spec divergence (documented in NOTES §3/§6):** route `requires_more_info` to the *earliest* step among all `requiredFields`, not literal `requiredFields[0]` — correct for multi-field cases; one-line revert path noted.

**Hygiene:** caught + untracked an accidentally-committed `coverage/` artifact; added to `.gitignore`.

**Not done (deliberate):** push/PR (user's own assessment repo — awaiting go-ahead); `/eval-tickets` (out of "build T2–T9" scope); on-device/sim smoke test (pure logic fully tested; UI projection unverified on a real device).

### Memory Consolidation
Mempalace wing `burjx`: 3 new drawers — `dev` (T2–T9 build complete + final state), `testing` (coverage gate excluded integration layer → all 3 QA-caught bugs lived there; resume/more_info/poll-error fixes + how to write the App integration test), `config` (expo `moduleDetection:force` needs explicit tsconfig `types`; fake-timer poll testing). Backed up to `.claude/tasks/lessons.md`. Auto-memory `project_burjx_kyc_phase0_state.md` updated to implementation-complete.
